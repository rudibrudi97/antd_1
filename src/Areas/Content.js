import React from "react";
import Stufe from "./Step_new";


 //const tableComponents = tableData.map(tab => <TableSorting name={tab.name} license={tab.license} /> )

function Content() {
    return(
        <div>
            <Stufe/>
        </div>
    )
}

export default Content